const _ = require('@sailshq/lodash');
module.exports.hookFactory = function (routes, hook) {
  if (_.isString(routes)) {
    routes = [routes];
  }
  return routes.reduce((combined, route) => {
    combined[route] = hook;
    return combined;
  }, {});
};

module.exports.policyFactory = function () {

};
